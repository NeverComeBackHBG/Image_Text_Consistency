# 图文相似度分析项目依赖

# 基础依赖
PyYAML>=6.0
requests>=2.28.0
numpy>=1.24.0
pillow>=9.0.0

# 中文分词（HanLP 或 jieba 作为备选）
hanlp>=2.0.0
jieba>=0.42.0

# BGE-M3 - 文本向量化
FlagEmbedding>=1.2.0

# Qwen2.5-VL - 图像处理（直接从HuggingFace下载）
torch>=2.0.0
transformers>=4.40.0
qwen-vl-utils>=0.0.2
accelerate>=0.25.0
bitsandbytes>=0.41.0  # 8-bit 量化，减少显存占用

# 可选：阿里云dashscope（如果使用API方式）
# dashscope>=1.14.0
