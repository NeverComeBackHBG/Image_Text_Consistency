# 图文相似度分析项目依赖

# 基础依赖
PyYAML>=6.0
requests>=2.28.0
numpy>=1.24.0
pillow>=9.0.0

# 中文分词
jieba>=0.42.0
# hanlp>=2.0.0  # 可选，如果需要更精确的中文分词

# BGE-M3 - 文本向量化
FlagEmbedding>=1.2.0

# Qwen2.5-VL - 图像处理
torch>=2.0.0
transformers>=4.40.0
qwen-vl-utils>=0.0.2
accelerate>=0.25.0

# 量化支持（减少显存占用）
bitsandbytes>=0.41.0

# 可选依赖
# peft>=0.7.0  # 如需LoRA微调
# opencv-python>=4.8.0  # 如需图像预处理
