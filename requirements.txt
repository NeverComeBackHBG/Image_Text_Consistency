# 图文相似度分析项目依赖

# 基础依赖
PyYAML>=6.0
requests>=2.28.0
numpy>=1.24.0
pillow>=9.0.0

# HanLP - 中文NLP（分词、词性标注）
hanlp>=2.0.0

# BGE-M3 - 文本向量化
FlagEmbedding>=1.2.0

# Qwen2.5-VL - 图像处理（直接从HuggingFace下载）
torch>=2.0.0
transformers>=4.40.0
qwen-vl-utils>=0.0.2
accelerate>=0.25.0

# 可选：阿里云dashscope（如果使用API方式）
# dashscope>=1.14.0
